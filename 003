#include <iostream>

int main()
{
    int n, edad, mayor = 0;
    std::cin >> n;

    for (int i = 1; i <= n; i++)
    {
        std::cin >> edad;
        if (edad > mayor)
            mayor = edad;
    }

    std::cout << mayor << std::endl;

    return 0;
}
